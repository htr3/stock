🚀 PHASE 1: Extract Raw Knowledge (From Books)
🟢 Prompt 1: Candlestick Patterns (Structured)
List all candlestick patterns mentioned in the uploaded books.

For each pattern, return:
- name
- type (bullish / bearish / neutral)
- conditions (strict rules using OHLC)
- reliability (if mentioned)
- ideal market condition (trend/reversal/sideways)

Return strictly in JSON format.
🟢 Prompt 2: Intraday Setups
List all intraday trading setups described in the books.

For each setup, return:
- name
- entry condition
- exit condition
- stop loss logic
- indicators used
- timeframe suitability (especially 5-min)

Return in JSON format.
🟢 Prompt 3: Indicators Logic
Extract all technical indicators mentioned in the books.

For each indicator, provide:
- name
- formula (mathematical expression)
- parameters (e.g., 14 period RSI)
- buy signal condition
- sell signal condition

Return in structured JSON.
🧠 PHASE 2: Convert Knowledge → ML Features
🟢 Prompt 4: Convert Patterns to Features
Convert the following candlestick patterns into machine learning features.

For each pattern:
- write logical conditions using OHLC columns
- output should be binary feature (1 or 0)
- assume dataframe columns: Open, High, Low, Close, Volume

Return Python pandas code for each feature.

👉 (Paste output from Prompt 1)

🟢 Prompt 5: Convert Indicators to Features
Convert the following technical indicators into ML features.

For each indicator:
- write pandas code
- include calculation
- include signal feature (buy/sell/neutral)

Return clean Python functions.

👉 (Paste output from Prompt 3)

🟢 Prompt 6: Combine Signals (VERY IMPORTANT)
Create combined trading features using:
- candlestick patterns
- indicators (RSI, EMA, MACD)
- volume

Generate features like:
- strong_buy_signal
- strong_sell_signal

Use logical combinations and return pandas code.
🔢 PHASE 3: Add Mathematical / Statistical Features
🟢 Prompt 7: Time Series Features
Generate time-series based features for stock prediction using OHLCV data.

Include:
- lag features (previous 1, 2, 3 candles)
- rolling mean
- rolling standard deviation
- returns (1-step, 3-step)

Return pandas code.
🟢 Prompt 8: Volatility & Statistics
Create statistical features for stock data.

Include:
- volatility (standard deviation)
- z-score
- price momentum
- moving average deviation

Return Python pandas code.
🎯 PHASE 4: Define Target (VERY IMPORTANT)
🟢 Prompt 9: Target Variable
Define multiple target variables for intraday stock prediction (5-min timeframe).

Include:
- next candle direction (classification)
- price change percentage
- breakout prediction

Return pandas code.
🧪 PHASE 5: Feature Selection (SMART FILTER)
🟢 Prompt 10: Feature Optimization
Given a dataset with multiple features, suggest how to:
- remove redundant features
- select important features
- avoid overfitting

Focus on stock prediction use case.
🔥 BONUS: PRO-LEVEL PROMPT (VERY POWERFUL)
🟣 Prompt 11: Quant-Level Feature Builder
Act as a quantitative trading expert.

From the uploaded books, generate:
- high probability trading features
- combinations of indicators + patterns
- features suitable for machine learning models

Focus on 5-minute intraday trading.

Return:
- feature name
- logic
- pandas implementation
🧠 How You Should Use This

👉 Workflow:

Run Prompt 1–3 → extract knowledge
Run Prompt 4–6 → convert to features
Run Prompt 7–8 → add math/statistics
Run Prompt 9 → define target
Train model
⚠️ Important Advice
Don’t use all features blindly ❌
Start with 10–15 features
Test → improve
🚀 Next Step

After you run these prompts, come back and say:

👉 “combine all features into one dataset”

I’ll help you:
✅ Build final dataset
✅ Train model properly
✅ Add backtesting (real profit check)

Now you’re building something serious 🔥 (quant + ML system)

🚀 STEP 1: Clean & Normalize Your LLM Output

Your outputs (from NotebookLM) might be messy.

👉 First, make sure each looks like this:

Candlestick Example:
{
  "name": "bullish_engulfing",
  "type": "bullish",
  "conditions": [
    "previous candle bearish",
    "current candle bullish",
    "current body engulfs previous body"
  ]
}

👉 Do this manually OR ask LLM:

Clean and normalize this JSON into consistent structure with:
name, type, conditions
🧠 STEP 2: Convert Patterns → Code (AUTOMATION PROMPT)

Now use this:

🔥 Prompt 4 (Use NOW)
You are a Python quantitative developer.

Convert the following candlestick patterns into pandas features.

Rules:
- Use dataframe columns: Open, High, Low, Close, Volume
- Use shift(1) for previous candle
- Each feature must return 1 or 0
- Keep logic strict and realistic for trading

Return only Python code.

Patterns:
<PASTE YOUR JSON HERE>
📈 STEP 3: Convert Indicators → Code
🔥 Prompt 5
You are a Python ML engineer.

Convert the following technical indicators into pandas features.

Requirements:
- Include calculation
- Include signal feature (buy/sell/neutral)
- Use standard formulas
- Optimize for 5-minute intraday data

Return Python functions.

Indicators:
<PASTE YOUR JSON HERE>
⚡ STEP 4: Convert Setups → Combined Features
🔥 Prompt 6
You are a quantitative trader.

Convert the following intraday setups into machine learning features.

Requirements:
- Combine indicators + price + volume
- Output binary or categorical features
- Suitable for 5-minute timeframe

Return pandas code.

Setups:
<PASTE YOUR JSON HERE>
🧪 STEP 5: Build Feature File (IMPORTANT)

Now create file:

👉 feature_engineering.py

Structure:

def add_all_features(df):
    # indicators
    # patterns
    # setups
    # stats

    return df
🔢 STEP 6: Add Math + Time Series (MANDATORY)

Even if books didn’t give this — ADD IT.

Use this prompt:

Generate additional time-series features for stock prediction:

Include:
- returns (1, 3, 5 candles)
- rolling mean (5, 10)
- rolling std (volatility)
- lag features (previous candles)
- volume change

Return pandas code.
🎯 STEP 7: Create Final Dataset

After all features:

df = add_all_features(df)

df["target"] = (df["Close"].shift(-1) > df["Close"]).astype(int)

df.dropna(inplace=True)
🤖 STEP 8: Train Model
features = [col for col in df.columns if col not in ["target"]]

X = df[features]
y = df["target"]
⚠️ IMPORTANT (Read This Carefully)

Right now:

You have too many features

👉 DO NOT use all

Start with:

2–3 patterns
2 indicators
1 setup
basic stats
🧠 Pro Insight (This is GOLD)

Books say:
👉 “Pattern works”

ML tells:
👉 “Pattern works only when combined with X + Y”

Example:

Bullish Engulfing alone → weak  
Bullish Engulfing + RSI < 30 + Volume spike → strong
🚀 YOUR NEXT STEP

Do this now:

Run Prompt 4 → get pattern code
Run Prompt 5 → get indicator code
Run Prompt 6 → get setup code